# 検証記録

2026-09-25、macOS / Node.js v22.22.3で確認。

| 対象 | 結果 |
| --- | --- |
| `npm ci` | 成功。インストール時のauditは0件 |
| `npm test` | 25テスト成功 |
| `npm run lint` | 成功 |
| 登録・ログイン・ログアウト | HTTPテストとブラウザ操作で確認 |
| 公開／非公開、所有者による編集 | HTTPテストで確認。ブラウザで投稿・編集・検索を確認 |
| 検索 | 日本語・引用符・ワイルドカード文字を通常の検索文字として扱い、表示可能な投稿だけを返すことを確認 |
| セッション | ローテーション、古いCookie、改変Cookie、CSRF、期限切れ、独立環境を確認 |
| 再起動 | 実子プロセスを停止・再起動し、登録アカウント・投稿・編集・旧Cookie・環境上限の初期化を確認 |
| 容量 | 上限2環境に5件同時アクセスし、新規3件が503、期限切れ後に利用を再開できることを確認 |
| 公開モード | Basic認証・必須設定・Secure Cookie・信頼プロキシのHTTP結合テスト成功 |
| 画面 | ノートPC幅でレイアウト、新規登録、誤パスワード、ログイン、投稿、編集、検索、ログアウトを確認。再起動後は初期画面に復帰 |
| `docker compose config --quiet` | 成功 |
| Docker build / up / restart / down | 未実施。Dockerデーモンが停止しており接続できない |

## レビューと修正

各Planで2名の読み取り専用レビューを実施。正確性・計画整合と、境界値・認証・エラー処理の観点を分担した。

- 失効済みセッションを遅延したレスポンスが復元しないよう、セッションストアを修正。
- 編集フォームで本文先頭の改行が消えないよう修正し、ブラウザとテストで確認。
- ホストに既存のBasic認証環境変数がある場合にも再起動テストが独立するよう、子プロセスの設定を固定。
- IPv4射影IPv6のCIDRで全IPv4を信頼できる設定を拒否。通常のIPv4/IPv6 CIDRと表記違いの回帰テストを追加し、対象3テストとlintで確認。

## 残件

Docker Engineの起動後に、運営・開発担当者がREADMEのbuild / up / restart / downを実行し、コンテナ内のネイティブDB依存と再起動を確認する。現時点でDockerの動作確認済みとは扱わない。

公開担当者は、ホスティング先・URL・想定人数を確定した後、実HTTPSとCookie、プロキシの送信元と転送ヘッダー上書き、配信元への直接アクセス制御、参加人数での性能、終了時の停止・削除を確認する。公開環境へのデプロイは未実施。

検索への意図的な脆弱性の追加は、今回の検証・実装範囲に含まない。

## 保存量上限の修正（2026-09-25）

Codex Securityの保存量無制限の指摘に対応。修正前は、投稿件数上限を指定した回帰テストが `Missing expected exception` で失敗し、超過書き込みが可能なことを確認した。

- DBの挿入時に投稿・アカウント総数を制限し、初期データ、全アカウント、公開／非公開を合算する。
- SQLiteは各環境8MiBまで。マルチバイト本文による容量到達と、容量不足の編集が元の行を変更しないことを実行確認した。
- HTTPでは超過時に507を返す。同時登録の最後の1枠、ログアウト・再ログイン、継続アクセスで件数上限を回避できないことを確認した。閲覧、件数上限での編集、別環境の操作も成功した。
- 一時コピーで `node --check`、対象6テスト、`npm test`（全31テスト）、`npm run lint` が成功。HTTP待受はサンドボックス制限により初回EPERMとなり、ローカル待受を許可した再実行で成功した。
- `docker compose config --quiet` が成功。今回もDockerイメージのbuild・実行と実公開環境の検証は行っていない。
- 修正差分の読み取り専用レビューで、具体的な回避経路・回帰の追加指摘なし。

環境枠の占有に関する指摘はユーザーの選択により保留。参加コードやIP制限は導入していない。DB容量制限はプロセス全体のRSS上限を保証しない。
